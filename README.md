# sqlipwn
this is automated methord for sql injection using sqlmap . each request can be inserted into a .txt (eg:common_get_initial.txt) file and the name can be adde into a masterfile (master_list.txt) that use the ltinary format to scan each request  contain in the master_list.txt file 
python3 auto_sql.py master_list.txt
